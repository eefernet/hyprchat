fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
aiosqlite==0.20.0
python-multipart==0.0.12
pypdf>=4.0.0
chromadb>=0.4.0
trafilatura>=1.10.0
PyYAML>=6.0
# Scanned-PDF OCR fallback (backend/ocr.py) — pure pip, CPU-only
rapidocr-onnxruntime>=1.3.0
pypdfium2>=4.30.0
# Jarvis scheduler (backend/scheduler.py) — cron-expression schedules
croniter>=2.0
# Jarvis calendar (backend/caldav_sync.py) — two-way CalDAV sync
caldav>=1.3
# Imported directly by caldav_sync.py (ICS build/parse) — pin explicitly
# instead of relying on it as a transitive dep of caldav.
icalendar>=5.0
# Jarvis email (backend/email_client.py) — IMAP + async SMTP
imap-tools>=1.7
aiosmtplib>=3.0
